// Funções

// Declarando a função | String
function escreveTexto(texto) {
    console.log(texto);
}

// A função pode ser chamada uma ou + vezes
escreveTexto("Oi eu sou Denilson");
escreveTexto("Seja bem vindo ao curso");

// Existe 3 formas de se escrever uma função
// 1 - Declaração de função -> Nome | Parametro | Retorno
// 2 - ArowFunction ->
// 3 - 

function soma() {
    return 2 + 2;
}
    
console.log(soma());

escreveTexto(soma());
