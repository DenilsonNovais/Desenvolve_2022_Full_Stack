// Ao adicionar um valor ao array, ele entrara como ultimo valor da lista
const notas = [10, 8, 6]; // Declarando o Array

notas.push(7);  // Adicionando valor ao array

console.log(notas); // Mostrando no console as alteração